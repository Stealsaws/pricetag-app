# วิธีเพิ่มเทมเพลตใหม่ในอนาคต

แอปนี้ออกแบบให้ **ไม่ต้องแก้โค้ด** เวลาเพิ่มเทมเพลตใหม่ — ทุกอย่าง (หมวด, ขนาด,
ตำแหน่งข้อความ, ฟอนต์, สี) อยู่ใน `data/templates.json` ไฟล์เดียว หน้าเว็บจะ
อ่านค่าจากไฟล์นี้แล้วสร้างปุ่มเลือกหมวด/ขนาด และฟอร์มกรอกข้อมูลให้เองอัตโนมัติ
ตาม field ที่เจอในเทมเพลตนั้นๆ

มี 2 กรณี: **(A)** ได้ไฟล์ .docx เทมเพลตใหม่มาจากทีมออกแบบ ให้ใช้สคริปต์ในโฟลเดอร์
`tools/` ช่วยแปลง หรือ **(B)** ต้องแก้ตำแหน่ง/เพิ่มฟิลด์เองแบบไม่มีไฟล์ Word
(แก้ JSON ตรงๆ)

---

## กรณี A: ได้ไฟล์ .docx เทมเพลตใหม่มา

### ขั้นตอนที่ 1 — ลองแปลงอัตโนมัติก่อน

```bash
cd tools
python3 extract_template.py path/to/Sign-ใหม่.docx \
  --template-id E-01 \
  --category E --category-label "หมวดใหม่" \
  --size-code 01 --size-label "A3 ตั้ง" \
  --app-dir ../ \
  --merge
```

- `--template-id` : รหัสเทมเพลต ตั้งเป็น `<หมวด>-<ขนาด>` เช่น `E-01`
- `--category` / `--category-label` : รหัส/ชื่อหมวดที่จะโผล่ในปุ่มเลือก
  (ถ้าใช้รหัสหมวดที่มีอยู่แล้ว เช่น `A`, จะไปรวมกับปุ่ม "ลดราคา" เดิม)
- `--size-code` / `--size-label` : เหมือนกันแต่สำหรับขนาดกระดาษ
- `--merge` : ให้เขียนผลลงใน `data/templates.json` ให้เลยอัตโนมัติ
  (ถ้าไม่ใส่ จะได้ไฟล์ `<template-id>.template.json` แยกไว้ให้ก็อปเอง)

สคริปต์จะ:
1. อ่านขนาดกระดาษ, ตำแหน่ง/ฟอนต์/สีของทุกกล่องข้อความจากไฟล์ Word ตรงๆ
2. เดาว่ากล่องข้อความไหนคือฟิลด์ไหน (ชื่อสินค้า/ราคา/ราคาปกติ/วันที่/ฯลฯ)
   จากรูปแบบข้อความตัวอย่างในเทมเพลต
3. ก็อปปี้รูปพื้นหลังไปไว้ที่ `assets/bg/`
4. เตือนถ้าเจอข้อความที่เดา role ไม่ได้ (จะขึ้น `UNKNOWN-...`) — ให้เปิด
   `tools/extract_template.py` แล้วเพิ่มเงื่อนไขใน `classify()` หรือแก้ role
   ในไฟล์ JSON ที่ได้เองก็ได้

### ขั้นตอนที่ 2 — ตรวจสอบด้วยตา

เปิดแอป (ตาม README.md หลัก) เลือกเทมเพลตใหม่ กรอกข้อมูลตัวอย่าง แล้วเทียบกับ
ไฟล์ Word ต้นฉบับว่าตำแหน่งตรงกันไหม ถ้าไม่ตรง ให้ไปแก้ตัวเลข `x_mm`/`y_mm`/
`w_mm`/`h_mm` ตรงในไฟล์ JSON ได้เลย (ดูหัวข้อ "โครงสร้างไฟล์ templates.json"
ด้านล่าง)

### ⚠️ ข้อควรระวัง: เทมเพลตที่พิมพ์ 2 ใบ/แผ่นขึ้นไป (เหมือน A5 นอน)

`extract_template.py` **หาตำแหน่งของสำเนาที่ 2 (และต่อๆไป) ผิดเสมอ** — นี่ไม่ใช่
บั๊ก แต่เป็นข้อจำกัดของ Word เอง (ตำแหน่งกล่องข้อความอ้างอิงจาก "paragraph"
ของมันเอง ไม่ใช่จากหน้ากระดาษ พอมีสำเนาที่ 2 ในพารากราฟถัดไป ระบบไม่มีทาง
รู้ว่าพารากราฟแรกกินพื้นที่ไปกี่ mm) เทมเพลต A5 ทั้ง 4 แบบในโปรเจกต์นี้ก็เจอ
ปัญหานี้เหมือนกัน — วิธีแก้คือ**วัดตำแหน่งจากรูปที่ render จริงด้วยตา**:

1. แปลง .docx เป็นรูปด้วย LibreOffice (ต้องติดตั้งฟอนต์ที่เทมเพลตใช้ก่อน
   ไม่งั้นตัวอักษรจะเพี้ยน):
   ```bash
   mkdir -p ~/.fonts && cp fonts/*.ttf ~/.fonts/ && fc-cache -f ~/.fonts
   libreoffice --headless --convert-to png --outdir ./preview path/to/Sign-ใหม่.docx
   ```
2. ใช้ `tools/grid_overlay.py` วางเส้นกริดทับรูป (หน่วย mm) เพื่ออ่านตำแหน่ง
   ด้วยตา:
   ```bash
   python3 tools/grid_overlay.py preview/Sign-ใหม่.png \
     --page-w-mm 210 --page-h-mm 297 --step-mm 10 \
     --crop-y-mm 30 130 \
     --out grid.png
   ```
   (`--crop-y-mm` ใส่ช่วง mm ของสำเนาที่ 1 เพื่อตัดมาดูเฉพาะอันเดียวก่อน)
3. เปิด `grid.png` อ่านตำแหน่ง/ขนาดของแต่ละกล่องข้อความจากเส้นกริด แล้วพิมพ์
   ค่าลงใน `templates.json` เอง (ดูตัวอย่างจริงจากเทมเพลต `A-04` ในไฟล์
   `data/templates.json` — มี `units` 2 ชุด แต่ละชุดมี `offset_y_mm` ต่างกัน)

ขั้นตอนนี้ใช้เวลาแต่แม่นยำ 100% เพราะวัดจากรูปที่ Word เรนเดอร์จริง ไม่ใช่
เดาจากตัวเลขในไฟล์ XML

---

## กรณี B: แก้ตำแหน่ง/เพิ่มฟิลด์ในไฟล์ JSON เอง (ไม่มีไฟล์ Word)

### โครงสร้างไฟล์ templates.json

```jsonc
{
  "A-01": {                          // key = template id, ตั้งเองได้อิสระ
    "category": "A",                 // รหัสหมวด — ใหม่ได้ ไม่ต้องแก้โค้ด
    "categoryLabel": "ลดราคา",       // ชื่อหมวดที่โชว์ในปุ่ม
    "sizeCode": "01",
    "sizeLabel": "A3 ตั้ง",
    "page_w_mm": 297, "page_h_mm": 420,   // ขนาดกระดาษจริง (mm) ที่จะพิมพ์
    "isPair": false,                       // true ถ้าพิมพ์มากกว่า 1 ใบ/แผ่น
    "units": [                              // 1 หน่วย = สัญลักษณ์ 1 ใบป้าย
      {
        "background": "assets/bg/A-01.jpeg",  // path รูปพื้นหลัง (relative)
        "bg_x_mm": 0, "bg_y_mm": 0,            // ตำแหน่งรูปพื้นหลังในหน่วยนี้
        "bg_w_mm": 297, "bg_h_mm": 420,
        "offset_x_mm": 0, "offset_y_mm": 0,    // ตำแหน่งของหน่วยนี้บนหน้ากระดาษ
        "fields": [                             // ช่องที่ผู้ใช้กรอกได้
          {
            "role": "productName",              // ดูรายการ role ด้านล่าง
            "x_mm": 78.95, "y_mm": 150.92,       // ตำแหน่ง (จากมุมซ้ายบนของ "หน่วย")
            "w_mm": 178.86, "h_mm": 26.88,
            "fontSizePt": 26, "color": "#000000", "bold": true,
            "align": "right",                    // left / right / center
            "sampleText": "ซันไลต์ เลมอน เทอร์โบ" // ข้อความตั้งต้น/ตัวอย่าง
          }
          // ... ฟิลด์อื่นๆ
        ],
        "statics": [],       // ข้อความคงที่ (แก้ไม่ได้) เช่น "*เงื่อนไข..."
        "decorations": []    // รูปทรงเรียบง่าย เช่นจุด/แท่งสีของ ":-"
      }
    ]
  }
}
```

**`role` ที่ระบบรู้จักแล้ว** (มีช่องกรอกสำเร็จรูปในฟอร์ม):
`productName`, `description` (2 บรรทัด: รายละเอียด + ขนาด/ปริมาณ), `price`,
`oldPrice`, `dateRange`, `disclaimer` (ข้อความคงที่), `static` (label คงที่
เช่น "เพียง")

**role ใหม่ที่ไม่อยู่ในลิสต์นี้ก็ใช้ได้ทันที** — ระบบจะสร้างช่องกรอกข้อความ
ธรรมดาให้เองโดยอัตโนมัติ (ดู `KNOWN_ROLES` ใน `js/app.js` ถ้าจะเพิ่ม
input แบบพิเศษ เช่น dropdown หรือช่องตัวเลขเฉพาะ ต้องแก้โค้ดในไฟล์นั้น
ในฟังก์ชัน `buildForm()`)

### เพิ่มหมวด/ขนาดใหม่

ไม่ต้องแก้ `js/app.js` เลย — แค่เพิ่ม template ใหม่ใน `templates.json` ที่มี
`category`/`categoryLabel` หรือ `sizeCode`/`sizeLabel` ใหม่ ปุ่มเลือกจะโผล่ขึ้น
เองตอนโหลดหน้าเว็บครั้งต่อไป (ถ้าอยากได้ sub-label ภาษาอังกฤษใต้ชื่อหมวด
แบบ "SALE" ใต้ "ลดราคา" ให้เพิ่มใน `CATEGORY_SUB_LABELS` ที่ต้นไฟล์
`js/app.js` — ส่วนนี้เป็นของแต่งเสริมอย่างเดียว ไม่มีก็ไม่พังอะไร)

### ทดสอบหลังแก้ไฟล์

```bash
python3 -m http.server 8000   # รันจากโฟลเดอร์ pricetag-app
```
เปิด `http://localhost:8000` แล้วลองเลือกเทมเพลตใหม่ดู

---

## เครื่องมือในโฟลเดอร์ tools/

| ไฟล์ | ใช้ทำอะไร |
|---|---|
| `extract_template.py` | แปลงไฟล์ .docx ใหม่เป็น template config อัตโนมัติ (แม่นยำสำหรับเทมเพลตที่พิมพ์ 1 ใบ/แผ่น) |
| `grid_overlay.py` | วางเส้นกริด mm ทับรูปที่ render แล้ว ใช้วัดตำแหน่งด้วยตาเวลา extract อัตโนมัติไม่ได้ (เช่นแบบ 2 ใบ/แผ่น) |

ทั้งสองไฟล์รัน `--help` เพื่อดูตัวเลือกทั้งหมดได้
